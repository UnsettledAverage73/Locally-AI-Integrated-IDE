
export const localdev = {
  commands: {
    registerCommand: (id: string, command: () => void) => {
      // This is a placeholder. The actual implementation will be in PluginManager.ts
    },
  },
};
